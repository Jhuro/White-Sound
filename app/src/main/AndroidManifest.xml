<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WhiteSound"
        tools:targetApi="31">
        <activity
            android:name=".actividades.InformacionActivity"
            android:exported="false" />
        <activity
            android:name=".actividades.JuegosActivity"
            android:exported="false" />
        <activity
            android:name=".actividades.EditarPartituraActivity"
            android:exported="false" />
        <activity
            android:name=".actividades.AjustesActivity"
            android:theme="@style/TemaAjustes"
            android:exported="false" />
        <activity
            android:name=".actividades.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>